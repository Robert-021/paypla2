services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    networks:
      - payplay-network
    restart: always
    environment:
      - NODE_ENV=production

networks:
  payplay-network:
    driver: bridge
